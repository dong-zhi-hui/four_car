<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dj.ssm.mapper.TruckMapper">

    <select id="findTruckByCount" resultType="java.lang.Integer">
        select count(0) from truck_space where car_status = #{saaa}
    </select>
</mapper>